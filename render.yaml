services:
  - type: web
    name: resume-ranker
    env: python
    rootDir: .
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn src.api.main:app --workers=1 --bind 0.0.0.0:$PORT"


